Drop reference PDF books or papers here before running Agent 0 (`tools/agent0_materials.py`).

This folder is intentionally empty in the base repo. Add PDFs as needed per video topic, then pass the path to Agent 0:

```bash
python tools/agent0_materials.py --topic "your topic" --pdf "materials/Your Book.pdf"
```
